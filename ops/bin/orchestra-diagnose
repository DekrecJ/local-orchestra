#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)
readonly APP_DIR=$(cd -- "$SCRIPT_DIR/../.." && pwd -P)
cd "$APP_DIR"
exec .venv/bin/python -m app.diagnostics
